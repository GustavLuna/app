import ItemListContainer from "./ItemListContainer";

const Main = (props) => {

    return (
        <main className="container">
              <ItemListContainer/>
        </main>
    );
}

export default Main;